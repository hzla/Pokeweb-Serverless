# Following Pokémon — current validation

| Profile | Bundled package | Base and full build | Installer and export | Human acceptance |
|---|---|---|---|---|
| Stock White 2 IRDO | 0.6.76-alpha | Passed | Passed | Pending cold boot |
| Stock Black 2 IREO | 0.6.42-alpha | Passed | Passed | Pending cold boot |
| Italian White 2 IRDI | 0.6.43-alpha | Passed | Passed | Pending cold boot |
| White2Upgrade | 0.7.37-alpha | Passed | Passed | Pending cold boot |

Each pinned-ROM build produced a grounded-followers base field/event pair and a full riding/Surf field/event pair, with one shared core module. The packaged instruction, import, hook-anchor and variant hook-contract checks passed for both pairs on all four profiles. The full-package CPU regressions cover conversations, scripted transitions, recall and movement hold, follower priority and lighting, land riding, Surf handoff, positioning, cycling, terrain, and teardown. Native services and graphics submission are modeled; no game emulator was run.

The sixth Options row is present in the shared core for both packages. Each profile's build checked the seven Options hook signatures independently, the native background adapters, and that the five retail Options setters preserve save bit 11 in the actual ARM9 binary. Packaged CPU tests covered staged On/Off changes, unchanged Confirm selection, button and touch navigation as the list scrolls, Confirm commit, the ROM-wide master switch, copying the native fifth-row tile art to the sixth row, and normal, highlighted, and scrolled background palettes. Field and Surf harnesses covered follower suppression/restoration and the custom-to-retail Surf teardown. Native menu services were mocked; on-screen layout, sounds, save/reload, mounted-menu transitions, and long-session allocation behavior still need human cold-boot checks.

For every profile, `verify-following-variants.ts` passed fresh base installation, full installation, removal and conversion back to base, export/reopen, enable/disable, walking-gap retention, and physical removal of mount data and NitroFS paths while preserving unrelated file IDs. It also preserved a pre-existing edit to an unrelated Options text entry while installing and removing the four owned entries. An older combined alpha upgraded as full. `verify-following-install.ts` passed update, disable/reenable, removal/reinstall, and authored land/Surf positioning on each versioned full ROM. The stock run also passed artwork replacement and anchor regeneration. Fresh base ROMs contain no Surf archive, Surf registry, rider art, or land anchors. Converted ROMs retain zero-byte reserved file IDs but no mount payloads under the original paths.

The riding option is unchecked for new installs. The installed variant remains fixed until removal; an older combined receipt defaults to full. Following enable/disable remains independent of that choice and preserves the save's On/Off setting. Both packages retain the current grounded follower art, walking-gap archive, dialogue, gifts, and saves. The [memory audit](MEMORY-AUDIT.md) measures a 268-byte increase in the shared core's fixed payload for the row styling. It reuses the native background buffer and adds no temporary allocation; the existing Options row still allocates 1,684 bytes for native text windows and two slots in its marker pool while open. The audit excludes allocator bookkeeping, stack, and VRAM.

The installer accepts data-edited ROMs whose follower hook sites, adapters, and archives still satisfy the profile contract. White2Upgrade additionally requires its audited expansion modules. The focused follower checks, privacy check, TypeScript check, and production build passed. The full web suite passed 1,429 tests with 3 skipped using a 10-second per-test timeout. Human visual and audio checks remain open for the Options row, grounded followers, shadows and lighting, land mounts, native retail Surf in base, custom Surf in full, transitions, and repeated mount/dismount cycles. Cold boot each versioned export with a matching ordinary save; older save states retain the previous loaded code. See [TEST-MAP.md](TEST-MAP.md) and the [human checklist](EMULATOR-CHECKLIST.md).
