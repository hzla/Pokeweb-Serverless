# Following Pokémon runtime

Pokeweb's dedicated Following Pokémon page installs three PMC modules for each supported profile. Current bundled versions are stock White 2 **0.6.76-alpha**, stock Black 2 **0.6.42-alpha**, Italian White 2 **0.6.43-alpha**, and White2Upgrade **0.7.37-alpha**. Stock games support species 1–649; White2Upgrade supports through 1023. Each profile has its own pinned binary contract, runtime fingerprint, and save family. Italian White 2 uses Italian generic dialogue; authored dialogue and gifts remain editable in Pokeweb.

New installs default to the **base** package: grounded followers, L/R cycling, dialogue, effects, scene safety, and walking-gap positioning. The install-time **Riding and custom Surf** option selects the **full** field/event DLL pair and adds the Surf archive, appearance registry, rider art, and anchors. Base has no riding hook, A+B handler, custom Surf renderer, or mount asset payload; retail Surf remains available. The shared core DLL is identical between variants. An installed choice is locked until the runtime is removed and reinstalled. Previously combined alpha installations upgrade as full. Conversion to base strips the old mount payloads and names on export while retaining reserved zero-byte file IDs to avoid renumbering unrelated ROM files.

The field module owns a temporary follower actor, a 64-record movement trail, identity validation, effects, and conversations. The full variant also owns land riding and custom Surf. The resident core extends object-code lookup without changing stock results; the events module handles safe-scene policy. No follower actor is saved. Menus, unchanged PC storage, signs, and safe stationary conversations retain the visible follower; unsafe scenes, battles, doors, and genuine actor conflicts recall it. Seamless map crossings should retain it. L/R cycles the visible party follower without reordering the party. In the full variant, A+B mounts a visible eligible follower; ordinary Surf prefers a Surf-capable selected follower, then the first non-Egg Surf knower in party order. The selected land follower returns after Surf. Repel continuation retains a follower or mount for either answer.

Each profile adds **FOLLOWERS: ON/OFF** to the sixth in-game Options row, localized as **SEGUACI: SÌ/NO** in Italian. The row uses the native menu font, row background and border, selection palette, markers, help window, and controls. Off is stored in reserved Options bit 11, so existing saves default to On. Changes are staged until Confirm accepts them; canceling restores the prior choice. Off suppresses grounded followers and riding and returns an active custom Surf ride to the retail mount while preserving Surf mode. The Pokeweb enable control remains a ROM-wide master switch: disabling it leaves the in-game row blank without changing the save bit. Removal restores the four owned Options text entries if they still match the installed text.

Recall holds directional input and defers the native field-script callback for 12 field updates on all four profiles. The follower's private recall image uses the light cyan and white of the send-out flash, pauses briefly, then shrinks into the existing ball effect. The cloned billboard retains its draw-enable and map-light bits and the follower's last submitted pose. The battle ball cue plays as the ball opens and when recall begins. The event resumes after recall; human cold-boot checks remain, especially at doors and scripted transitions.

Walking and mounted artwork use a draw-only depth correction to clear their native ground shadow. A follower north of the player remains behind the player after that correction for all four player facings; north, side, and stair policies are separate. The player-depth correction runs only for followers close enough to overlap the player and stops short of nearby native NPCs in front. The player's map light selection is mirrored on follower and mounted artwork while preserving native scene colors. Drawn quads and light flags are restored after synchronous submission, leaving world position, collision, shadow anchors, saves, and the movement trail unchanged. Visual acceptance is pending. A saved emulator state contains its old loaded code: cold boot a new export with the matching ordinary save.

All four profiles keep the one-pixel paired mount/rider stride going while stopped, without moving the ground shadow. Their automatic left/right follower gap is the existing artwork-based 0–6 units plus six units; manually authored gaps remain absolute overrides. When a visible follower is just beyond dialogue reach in the player's facing direction, its actor and drawn art move together back to that limit; the recorded trail and authored gap stay intact. The appearance editor offers up, down, and shared left/right walking gaps from 0–12, plus rider X/Y adjustments for land and Surf mounts. The four-direction previews use ROM player artwork and respond to edits immediately. Values are authored in the workspace and installed in `following/positioning.narc`; the runtime reads just the selected 12-byte land row or 8-byte Surf row from ROM.

## Assets and authoring

The editor's asset workspace stores validated appearance data under `following/`. Species, form, gender, and shiny variants are independently replaceable. Imported PNG sheets have two columns (idle and step) and four rows (up, down, left, right): 64×128 pixels for 32-pixel poses or 128×256 for 64-pixel poses. Each pose has transparency and at most 15 opaque BGR555 colors. Explicit source mapping is required; the importer does not guess numbered forms. Gen 5 artwork and optional Gen 6–9 artwork, Surf mounts, and land-rider art are separate bundled resources.

Conditional dialogue and one-time gifts live in follower-owned NARCs and can be edited without recompiling PMC code. Gifts have ten stable per-Pokémon claim slots; any number of ordered rules may reuse those slots within archive capacity. The installer accepts modified revision-0 ROMs when their audited code sites and follower archive layout still match; a whole-ROM clean SHA is not required. It owns its files by fingerprint, supports recognized alpha updates and enable/disable, and leaves authored data in place when disabled. Removal writes inert modules at the owned file IDs; shared PMC and imported assets remain.

## Build and checks

From the Pokeweb repository root, supply an exact pinned clean ROM:

```sh
npm run following:build -- /path/to/cleanwhite2.nds
npm run following:build-black2 -- /path/to/cleanblack2.nds
npm run following:build-italy -- /path/to/cleanwhite2-italy.nds
npm run following:build-upgrade -- /path/to/White2Upgrade.nds
npm run following:test
npm run build
```

Add `--publish` to a profile build to replace both bundled variant pairs and their receipt only after binary and packaged-code checks pass. `following:build-rom` installs the base package into a test ROM without launching an emulator; pass `--riding` for full. It copies the versioned alpha ROM to `Repos/` and preserves existing saves. Manual test output defaults outside this source tree. Python tooling uses `ndspy`, Pillow, Unicorn, Capstone, and `pyelftools`; the existing ignored `build/python` dependency directories remain supported. Set `ARM_TOOLCHAIN_BIN`, `RPM_TOOL_JAR`, or `FOLLOWING_BUILD_ROOT` when using nondefault local tools or output paths.

[VALIDATION.md](VALIDATION.md) records current automated evidence and its limits. [EMULATOR-CHECKLIST.md](EMULATOR-CHECKLIST.md) and the [Black 2](BLACK2-CHECKLIST.md), [Italian](ITALY-CHECKLIST.md), and [White2Upgrade](WHITE2UPGRADE-CHECKLIST.md) checklists are human cold-boot cases, not automated passes. [MEMORY-AUDIT.md](MEMORY-AUDIT.md) distinguishes fixed PMC payload from native graphics, stack, and VRAM use. The [test map](TEST-MAP.md) identifies build gates and manual diagnostics. Past alpha narratives remain in Git history.

## Limits

Human timing, sound, terrain, stair, occlusion, crowded-map allocation, and long-session checks remain necessary. Unsupported binary signatures are rejected. Stock ROMs cannot use later-generation species or art. Imported resources do not imply that every form has unique artwork. Retail content and unrelated PMC patches require their own compatibility checks. No current-profile full game-emulator acceptance is claimed by the packaged ARM tests.
