# Follower memory footprint

Generated by `audit_memory.py` from the packaged DLLs, matching debug ELFs and delivered ROM data.

| Profile | Previous full | Base fixed payload | Full fixed payload | Full − base | Species index / page cache | Generic capacity / data | Context capacity / data | Gift scratch / data |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| stock 0.6.76-alpha | 67,956 B | 55,092 B | 68,224 B | 13,132 B | 1,302 / 1,024 B | 8,192 / 3,852 B | 4,096 / 76 B | 4,096 / 76 B |
| black2 0.6.42-alpha | 67,956 B | 55,092 B | 68,224 B | 13,132 B | 1,302 / 1,024 B | 8,192 / 3,852 B | 4,096 / 76 B | 4,096 / 76 B |
| white2upgrade 0.7.37-alpha | 68,944 B | 56,080 B | 69,212 B | 13,132 B | 2,050 / 1,024 B | 8,192 / 3,852 B | 4,096 / 76 B | 4,096 / 76 B |
| white2italy 0.6.43-alpha | 68,132 B | 55,264 B | 68,400 B | 13,136 B | 1,302 / 1,024 B | 8,192 / 3,668 B | 4,096 / 76 B | 4,096 / 76 B |

The fixed payload is packaged code plus initialized data and BSS, including
the index, cache and conversation buffer. Neither new nor preceding release
allocates a separate registry. Loader bookkeeping, allocator overhead,
stack, native actor/effect/message allocations and VRAM remain separate.
These numbers are not whole-game peak heap measurements. A fresh base
installation omits the Surf and land-rider archives, their registries and
anchors, and the associated resident textures.

## In-game Options row

The installed resident core grows by stock 268 B, black2 268 B, white2upgrade 268 B, white2italy 268 B compared with the preceding alpha cores. This is code, initialized data, and BSS;
base and full share the same core. The row styling reuses the retail BG
screen buffer and adds no temporary allocation. While Options is open,
the three existing native text windows allocate 1,684 bytes: 50 four-bit
tiles (1,600 bytes), three 16-byte
window records, and three 12-byte bitmap records. Two selection markers use
two slots in the menu’s existing actor pool. Closing Options releases the
windows and markers. These sizes exclude allocator bookkeeping and native
menu allocations already present in the retail game.

The stock Surf catalog contains 1,455 ROM-resident appearance records in a 11,656-byte index and a 18,339,700-byte archive. It allocates no permanent index buffer; only the chosen mount's sixteen textures are loaded. Native graphics allocations and VRAM are outside the fixed-payload total.
The black2 Surf catalog contains 1,455 ROM-resident appearance records in a 11,656-byte index and a 18,339,700-byte archive. It allocates no permanent index buffer; only the chosen mount's sixteen textures are loaded. Native graphics allocations and VRAM are outside the fixed-payload total.
The white2upgrade Surf catalog contains 2,324 ROM-resident appearance records in a 18,608-byte index and a 29,081,396-byte archive. It allocates no permanent index buffer; only the chosen mount's sixteen textures are loaded. Native graphics allocations and VRAM are outside the fixed-payload total.
The white2italy Surf catalog contains 1,455 ROM-resident appearance records in a 11,656-byte index and a 18,339,700-byte archive. It allocates no permanent index buffer; only the chosen mount's sixteen textures are loaded. Native graphics allocations and VRAM are outside the fixed-payload total.

The stock land mount stores 2,574 eight-byte rider anchors in 20,608 ROM bytes and animated bike-free rider art in a 17,236-byte archive. It reads one anchor record on mount and loads twelve rider textures only while mounted. The existing follower billboard supplies the Pokémon artwork; native graphics allocations and VRAM are not included in fixed payload.
The black2 land mount stores 2,574 eight-byte rider anchors in 20,608 ROM bytes and animated bike-free rider art in a 17,236-byte archive. It reads one anchor record on mount and loads twelve rider textures only while mounted. The existing follower billboard supplies the Pokémon artwork; native graphics allocations and VRAM are not included in fixed payload.
The white2upgrade land mount stores 4,718 eight-byte rider anchors in 37,760 ROM bytes and animated bike-free rider art in a 17,236-byte archive. It reads one anchor record on mount and loads twelve rider textures only while mounted. The existing follower billboard supplies the Pokémon artwork; native graphics allocations and VRAM are not included in fixed payload.
The white2italy land mount stores 2,574 eight-byte rider anchors in 20,608 ROM bytes and animated bike-free rider art in a 17,236-byte archive. It reads one anchor record on mount and loads twelve rider textures only while mounted. The existing follower billboard supplies the Pokémon artwork; native graphics allocations and VRAM are not included in fixed payload.

The stock positioning NARC is 42,612 ROM bytes. The field code reads only a 12-byte land row or an 8-byte Surf row for the chosen appearance; it retains no extra catalog buffer in PMC heap.
The black2 positioning NARC is 42,612 ROM bytes. The field code reads only a 12-byte land row or an 8-byte Surf row for the chosen appearance; it retains no extra catalog buffer in PMC heap.
The white2upgrade positioning NARC is 75,292 ROM bytes. The field code reads only a 12-byte land row or an 8-byte Surf row for the chosen appearance; it retains no extra catalog buffer in PMC heap.
The white2italy positioning NARC is 42,612 ROM bytes. The field code reads only a 12-byte land row or an 8-byte Surf row for the chosen appearance; it retains no extra catalog buffer in PMC heap.

The 64-record trail replaces the earlier 256-record trail. Each 28-byte record stores
a sampled world position and route metadata. The follower sidecar shrinks from
7,228 to 1,852 bytes in the earlier packages. All current profiles use
1,856 bytes for four directional positioning values.
A full trail still recalls and reseeds safely; slow movement and complex paths
require human emulator acceptance to confirm the smaller bound is sufficient.

## Implemented behavior

- The complete appearance registry stays in ROM: 61,808 bytes for stock and
  56,648 bytes for Upgrade. No full-registry buffer or heap allocation remains.
- Initialization streams validation and both checksums through a 1 KiB cache,
  builds 16-bit species row boundaries, then publishes resident lookup bounds.
- Selection reads only the relevant species pages. Unown in the stock 24-byte
  format spans pages; lookup scoring remains identical across boundaries.
  Cache hits do not read the file. Movement and drawing perform no registry I/O.
- Invalid, short or missing data suppresses following safely. Every opened
  file is closed. Unload clears cached state and the resident extension bounds.
- The resident bridge is ABI 2. Its reader callback is synchronous and is
  never retained after initialization. Core/field updates are staged together.
- Generic conversation capacity is 8,192 bytes. Build, installation and runtime reject
  oversized data rather than truncating it; current messages use 3,852 bytes.
- Contextual dialogue and one-time gifts share one 4,096-byte fixed NARC scratch buffer; gifts load first and dialogue replaces it only when no unclaimed gift matches. It is loaded only
  when the first follower conversation is attempted, with no PMC shared-heap allocation.
- Upgrade reaction selection now accepts species 1–1023; stock remains 1–649.
  Egg IDs remain excluded. Actual selected species reaches the native cry
  call; this change does not install additional cry assets.

All audited builds still use `-Os` and RPM `--strip`. Packaged symbol names are
stripped; exports/import hashes and required relocations remain. Debug ELFs
are not installed into ROM. History capacity and drawing corrections are unchanged; trail distance now accounts for sprite width.

## Verification and limits

Stock and Upgrade packaged CPU tests compare every installed appearance and fallback,
exercise 100 load/cache/unload cycles per profile, inject 16 file/data failures,
and check all 65,536 object codes with zero registry allocations. Black 2
passed its exact-ROM contract and packaged branch checks. Native FS seek
instructions execute against a mocked synchronous filesystem dispatcher.
An earlier isolated regression using the supplied Upgrade state retained 100,332 bytes
free on application heap 1 before, during and after registry setup. This
does not measure native rendering allocations or PMC loader peaks.
Host reaction tests cover both species limits and exact 8 KiB data boundaries.
The Upgrade 100-conversation CPU test includes species 650, 722, 810 and 1023.
See VALIDATION.md for installer/build evidence and the human checklists for
cold boot, PC changes, conversations, forms and repeated transitions.
No game emulator or hardware was run for these releases.
